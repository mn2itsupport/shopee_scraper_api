fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
supabase==2.7.4
httpx==0.27.2
playwright==1.49.1
playwright-stealth==2.0.3
# shopee_th's persistent-profile browser only (see browser_pool.py's
# _shopee_th_context) — a patched Playwright fork that avoids the CDP
# Runtime.enable/Console.enable leaks and automation command-line flags
# playwright-stealth doesn't cover. Confirmed via live testing that Shopee
# TH's anti-bot layer invalidates an authenticated session on the very first
# plain-Playwright-driven action after a real human login, regardless of
# destination page, process boundary, proxy/IP, or fingerprint alignment —
# this targets that specific detection vector. Replaces playwright-stealth
# for shopee_th specifically; every other site keeps plain Playwright.
patchright==1.49.1
jinja2==3.1.4
# shopee_th's curl_cffi transport only (see ShopeeTHScraper.fetch_pdp_via_curl_cffi /
# ShopeeScraper._curl_cffi_fetch) — a plain HTTP client that impersonates a
# real Chrome TLS/JA3 fingerprint. Confirmed via live testing to get past the
# risk-control wall that blocks shopee_th's page load on both
# brightdata_unlocker_api (CaptchaBlockedError) and a cold Apify cache miss —
# recovers title/images from the page's own embedded mfe-initial-data
# snapshot. Does NOT get price/rating/sold_count: a direct call to the
# pdp/get_pc API this data actually lives in is confirmed risk-control-
# rejected (error 90309999, same code _parse_api_body already handles)
# regardless of session cookies/headers — that endpoint needs a real browser
# actually executing Shopee's anti-bot JS, which is out of scope here.
curl_cffi==0.16.3
