fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
supabase==2.7.4
httpx==0.27.2
playwright==1.47.0
playwright-stealth==2.0.3
# shopee_th's persistent-profile browser only (see browser_pool.py's
# _shopee_th_context) — a patched Playwright fork that avoids the CDP
# Runtime.enable/Console.enable leaks and automation command-line flags
# playwright-stealth doesn't cover. Confirmed via live testing that Shopee
# TH's anti-bot layer invalidates an authenticated session on the very first
# plain-Playwright-driven action after a real human login, regardless of
# destination page, process boundary, proxy/IP, or fingerprint alignment —
# this targets that specific detection vector. Replaces playwright-stealth
# for shopee_th specifically; every other site keeps plain Playwright.
patchright==1.62.3
jinja2==3.1.4
